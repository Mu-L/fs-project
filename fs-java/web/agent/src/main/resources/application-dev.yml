server:
  port: 7827
spring:
  ai:
    mcp:
      server:
        name: fs-mcp-server
        version: 1.0.0
        type: SYNC
        instructions: "MCP Server 示例"
        capabilities:
          tool: true
          resource: true
          prompt: true
          completion: true
fs:
  agent:
    # 模型网关认证密钥：大语言模型等节点调用模型网关时使用，整个 agent 服务共用
    token: sk-just-for-knowledge-dev
    image:
      expire: 1800000
      placeholder: /images/loading.svg
